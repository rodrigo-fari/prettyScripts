# Define zsh path:
export ZSH="$HOME/.oh-my-zsh"
export PATH="$HOME/.local/bin:$PATH"

# Set zsh theme:
ZSH_THEME="cypher"

# Test implementations:
COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
COMPLETION_WAITING_DOTS="true"

# Zsh plugins:
plugins=(git)
source $ZSH/oh-my-zsh.sh

# Alias:
alias config="vim ~/.zshrc"
alias restart="source ~/.zshrc"
alias c="cc -Wall -Wextra -Werror"
alias open="open ."
alias rm="rm -rf"
alias celar="clear"
alias m="make && ./minishell"
alias code='(cd "$VSCODESRC" && ./code "$OLDPWD")'
alias list="ls | sort"
alias kittyconfig="vim ~/.config/kitty/kitty.conf"
alias kitty="setsid $KITTYSRC/kitty.app/bin/kitty >/dev/null 2>&1 < /dev/null &"
